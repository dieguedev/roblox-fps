-- Placeholder balance numbers -- no shop costs exist yet (Paso 7), so these
-- are round tunable values, not derived from anything. Revisit once weapon/perk
-- prices are defined so a kill-to-purchase pace can actually be tuned against them.
local GoldConfig = {
    KillGold = 10,
    HeadshotBonusGold = 5,
}
return GoldConfig
